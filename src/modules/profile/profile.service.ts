import { Prisma, Profile } from "@prisma/client";
import { prisma } from "../../config/db";

const createProfile = async(payload:Prisma.ProfileCreateInput):Promise<Profile>=>{
    // const profile = await prisma.profile.create({
        
    // })
}